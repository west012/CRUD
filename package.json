{
  "name": "Textbook-repo",
  "version": "1.0.0",
  "description": "create table library(\r id serial PRIMARY KEY,\r title VARCHAR(255) NOT NULL,\r author VARCHAR(255) NOT NULL,\r year INT NOT NULL,\r status BOOLEAN)",
  "main": "index.js",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.3",
    "pg": "^8.3.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
